[target]
me@myhost.net

[target:vars]

### Adjust the following variables:

instance_name=testing
domain=mywebsite.net
db_password="<< your password here >>"
admin_user=admin
admin_password="<< your password here >>"
admin_email="me@testing.net"
http_prefix=https
page_title="Grünes Brett {{ instance_name }}"

# The following information is required when email are to be sent via
# an SMTP server which is set up in setup_email_smtp.yml
email_address=
email_password=
email_smtp_host=
email_smtp_port=
email_user=

### Changing the following variables is optional:

target_www_dir=/var/www/{{ instance_name }}
wordpress_dir={{ target_www_dir }}/wordpress
vhost_file={{ instance_name }}_vhost.conf

db_name=gb_{{ instance_name }}
db_user=gb_{{ instance_name }}