<VirtualHost *:80>
        ServerName {{ domain }}

        DocumentRoot {{ wordpress_dir }}

        <Directory {{ wordpress_dir }}>
            Options Indexes FollowSymLinks MultiViews
            AllowOverride All
            Order allow,deny
            allow from all
        </Directory>

</VirtualHost>
